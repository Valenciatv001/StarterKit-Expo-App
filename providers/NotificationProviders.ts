import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NotificationProviders = () => {
  return (
    <View>
      <Text>NotificationProviders</Text>
    </View>
  );
};

export default NotificationProviders;

const styles = StyleSheet.create({});
